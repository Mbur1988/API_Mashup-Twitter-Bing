extends layout

block content
  h1 Top 10 Trending in #{location}
  //- #{JSON.stringify(articles)}
  each article in articles
    .item
      button.articleTitle #{article.article.replace(/_/g," ")}
  
  script(src='/javascripts/summary.js')
